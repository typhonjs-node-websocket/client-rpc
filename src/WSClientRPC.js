import WSClient from '@typhonjs-websocket/client';

/**
 * Provides an implementation of the 'typhonjs-rpc' sub-protocol for method and eventbus RPC w/ `server-rpc` module.
 */
export default class WSClientRPC extends WSClient
{
   /**
    *
    */
   onInitialize()
   {
   }
}
